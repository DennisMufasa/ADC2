language: python
python:
  - "3.6"

install:
  - pip install -U pip

before_script:
  - pip install -r requirements.txt

script:
  - python api/v1/tests/unit_tests/test_views.py

# safelist
branches:
  only:
    - ft-build-v1
